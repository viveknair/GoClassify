Limitations:
		Only on types defined within
		this current package
			(which means that you cant do it on
			 basic types and external packages)

		You cannot temporarily have variables that
		are not used (even for debugging purposes)
